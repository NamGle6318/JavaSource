package extend;

// 관계 : hasa, isa
// isa 사용 예쩡
// 상속은 단일상속만허용된다.
// class Child extends (Parent, Tv) (x) = 부모가 더블(xxxx)

// Object 클래스 - 모든 클래스의 조상
//                 자동적으로 무조건 object 클래스를 상속받음
public class Parent {
    int age;
}
