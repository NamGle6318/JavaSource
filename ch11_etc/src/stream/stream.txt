Stream
    - 컬렉션(배열 포함)의 저장 요소를 하나씩 참조해서 람다식으로 처리할 수 있도록 도와주는 반복자
    - 내부반복자 사용
    - 한번 소비되면 끝. 재사용불가
    - 중간처리(매핑, 필터링, 정렬)와 최종처리(반복, 카운팅, 합계,평균)가 가능
        -- 중간처리
            -- filter : 조건 필터링
            -- distinct : 중복제거
            -- map() : 스트림의 요소를 변환 
                -- mapToInt() : R = int
        -- 최종처리
            -- forEach() 
            -- collect()
